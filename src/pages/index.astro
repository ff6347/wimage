---
import CameraCapture from "../components/camera-capture.astro";
import Footer from "../components/Footer.astro";
import "../styles/shared.css";
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>WImage</title>
	</head>
	<body>
		<h1>WImage</h1>
		<p>
			Capture an image from your camera and and we tell you something about it
		</p>
		<CameraCapture />
		<Footer />
	</body>
</html>

<style>
	h1 {
		color: #333;
	}

	p {
		color: #666;
	}
</style>

<script>
	console.debug("debug at:");
	console.debug(
		`${location.protocol}//${location.hostname}${location.port ? `:${location.port}` : ""}/test-visualization-stream`,
	);
	console.debug(
		`${location.protocol}//${location.hostname}${location.port ? `:${location.port}` : ""}/test-summary-stream`,
	);

	console.info("--------------------");
	console.info("demo at:");
	console.info(
		`${location.protocol}//${location.hostname}${location.port ? `:${location.port}` : ""}/demo`,
	);
</script>
